geopandas>=0.14
shapely>=2.0
duckdb>=1.0
mercantile>=1.2
mapbox-vector-tile>=2.2
requests>=2.31
python-dotenv>=1.0
folium>=0.18
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
h3>=4.0
PyYAML>=6.0                # config file parsing
pyproj>=3.6                # google_traffic.py: WGS84 ↔ Web Mercator
scipy>=1.11                # google_traffic.py: cKDTree for pixel/edge matching
Pillow>=10.0               # google_traffic.py: read Playwright PNG screenshot
playwright>=1.40           # google_traffic.py: render Google Maps headless
osmnx>=1.9                 # notebook 0: fetch boundaries by name from OSM

# After pip install, also run:
#   playwright install chromium

# duckOSM — install from the standalone repo:
#   pip install -e ../duckOSM
# or directly from GitHub:
#   pip install git+https://github.com/Khoshkhah/duckOSM.git

# osmium-tool — system package (not pip):
#   Ubuntu/Debian : sudo apt install osmium-tool
#   macOS         : brew install osmium-tool
