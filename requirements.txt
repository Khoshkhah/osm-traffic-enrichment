geopandas>=0.14
shapely>=2.0
duckdb>=1.0
mercantile>=1.2
mapbox-vector-tile>=2.2
requests>=2.31
python-dotenv>=1.0
folium>=0.18
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
h3>=4.0
osmnx>=1.9                 # notebook 0: fetch boundaries by name from OSM

# duckOSM — install from the h3-routing-platform repo:
#   pip install -e ../h3-routing-platform/tools/duckOSM

# osmium-tool — system package (not pip):
#   Ubuntu/Debian : sudo apt install osmium-tool
#   macOS         : brew install osmium-tool
