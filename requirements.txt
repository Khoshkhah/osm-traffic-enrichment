geopandas>=0.14
shapely>=2.0
duckdb==1.5.2               # pinned: MotherDuck cloud requires <=1.5.2; MotherDuck access is built into duckdb via the md: connection string, no separate package needed
mercantile>=1.2
mapbox-vector-tile>=2.2
requests>=2.31
python-dotenv>=1.0
folium>=0.18
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
h3>=4.0
PyYAML>=6.0                # config file parsing
pyproj>=3.6                # google_traffic.py: WGS84 ↔ Web Mercator
scipy>=1.11                # google_traffic.py: cKDTree for pixel/edge matching
Pillow>=10.0               # google_traffic.py: read Playwright PNG screenshot
playwright>=1.40           # google_traffic.py: render Google Maps headless
osmnx>=1.9                 # notebook 0: fetch boundaries by name from OSM
timezonefinder>=6.0        # pipeline_network step [3]: IANA timezone from centroid

# After pip install, also run:
#   playwright install chromium

joblib>=1.3                # network_matching: parallel route matching

# duckOSM — install from the standalone repo:
#   pip install -e ../duckOSM
# or directly from GitHub:
#   pip install git+https://github.com/Khoshkhah/duckOSM.git

# network-matching — route-based map-matcher (scripts/route_match.py); install from sibling repo:
#   pip install -e ../network-matching
# or directly from GitHub:
#   pip install git+https://github.com/Khoshkhah/network-matching.git

# roadstyle — OSM road palette for scripts/build_match_explorer.py (investigation HTML only):
#   pip install -e ../roadstyle

# osmium-tool — system package (not pip):
#   Ubuntu/Debian : sudo apt install osmium-tool
#   macOS         : brew install osmium-tool
